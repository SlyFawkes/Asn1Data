# frozen_string_literal: true

RSpec.describe Asn1Data do
  it "has a version number" do
    expect(Asn1Data::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
